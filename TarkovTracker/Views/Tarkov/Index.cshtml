@using TarkovTracker.Data.Entities
@model IEnumerable<UserDataItem>

<h2>Трекер предметов</h2>

<div class="item-list">
    @foreach (var item in Model)
    {
        <div id="@item.Id" class="item">
            @{
                await Html.RenderPartialAsync("_TrackerItem", item);
            }
        </div>
    }
</div>

<script>
    function increaseItem(id) {
        $.ajax({
            url: '@Url.Action("IncreasePartial", "Tarkov")',
            type: 'GET',
            data: {
                "id": id,
            },
            success: function(item) {
                document.getElementById(id).innerHTML = item;
            },
            error: function() {
                alert('Fail to load user item')
            }
        });
    }
    
    function decreaseItem(id) {
        $.ajax({
            url: '@Url.Action("DecreasePartial", "Tarkov")',
            type: 'GET',
            data: {
                "id": id,
            },
            success: function(item) {
                document.getElementById(id).innerHTML = item;
            },
            error: function() {
                alert('Fail to load user item')
            }
        });
    }

</script>
